<#macro formInput id name  label="" type="text" value="" placeholder="">
    <label for="${id}">${label}</label>
    <input type="${type}" id="${id}" name="${name}" value="${value}" placeholder="${placeholder}">
</#macro>